| 工具名称                    | 工具功能                                                                      | 核心输入参数                                                                                                                                  | 关键返回内容                                                                           |
| ----------------------- | ------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| `numa_bind_docker_tool` | 使用 `numactl` 将指定 NUMA 绑定参数插入到镜像原有的 ENTRYPOINT / CMD 前，运行 Docker 容器（本地/远程） | - `image`：镜像名称<br>- `cpuset_cpus`：允许使用的 CPU 核心范围<br>- `cpuset_mems`：允许使用的内存节点<br>- `detach`：是否后台运行容器（默认 False）<br>- `host`：远程主机名/IP（可选） | - `status`：操作状态（success / error）<br>- `message`：操作结果信息<br>- `output`：命令的原始输出（如有） |
