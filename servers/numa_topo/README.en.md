| Tool Name        | Tool Function                                                                     | Core Input Parameters                                                      | Key Return Content                                                                                                                                                                                          |
| ---------------- | --------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `numa_topo_tool` | Uses `numactl` to obtain NUMA topology information from a local or remote machine | - `host`: Remote host name or IP (optional, leave empty for local machine) | - `nodes_total`: Total number of nodes<br>- `nodes`: List of node information, each node contains: `node_id` (Node ID), `cpus` (List of CPUs), `size_mb` (Memory size in MB), `free_mb` (Free memory in MB) |
