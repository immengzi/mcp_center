| 工具名称              | 工具功能                                  | 核心输入参数                         | 关键返回内容                                                                                                                                   |
| ----------------- | ------------------------------------- | ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `lscpu_info_tool` | 使用 `lscpu` 命令获取本地或远程主机的 CPU 架构及核心静态信息 | - `host`：远程主机名/IP（若不提供则获取本机信息） | `architecture`：CPU 架构（如 x86\_64）、`cpus_total`：CPU 总数量、`model_name`：CPU 型号名称、`cpu_max_mhz`：CPU 最大频率 (MHz)、`vulnerabilities`：常见安全漏洞的缓解状态字典 |
