# Sed文本处理MCP（管理控制程序）规范文档

## 一、服务介绍
本服务是一款基于`sed`命令实现文本处理的MCP（管理控制程序），核心功能包括在指定文件中进行文本替换和行删除操作。支持本地和远程主机文件操作，可以使用各种sed选项（如直接修改文件等）进行高级文本处理。

## 二、核心工具信息
| 工具名称 | 工具功能 | 核心输入参数 | 关键返回内容 |
| ---- | ---- | ---- | ---- |
| `sed_text_replace_tool` | 在文件中替换指定模式的文本 | - `host`：远程主机名/IP（本地操作可不填）<br>- `options`：sed选项（可选），如"-i"直接修改文件<br>- `pattern`：要替换的模式（支持正则表达式）<br>- `replacement`：替换后的文本<br>- `file`：要操作的文件路径 | 布尔值，表示操作是否成功 |
| `sed_text_delete_tool` | 删除文件中匹配模式的行 | - `host`：远程主机名/IP（本地操作可不填）<br>- `options`：sed选项（可选），如"-i"直接修改文件<br>- `pattern`：要删除的行的模式（支持正则表达式）<br>- `file`：要操作的文件路径 | 布尔值，表示操作是否成功 |

## 三、待开发需求
计划增加更多sed命令功能支持，如文本插入、行号处理等，进一步完善文本处理能力。