# 交换空间信息采集MCP（管理控制程序）规范文档
## 一、服务介绍
本服务是一款基于`swapon`命令实现进程信息采集的MCP（管理控制程序），核心功能为​​激活和管理交换空间（Swap）​，包括 ​​启用 Swap 设备/文件​​和查看当前 Swap 状态

## 二、核心工具信息
| 工具名称 | 工具功能 | 核心输入参数 | 关键返回内容 |
| ---- | ---- | ---- | ---- |
| `swapon_collect_tool` | 获取目标设备（本地/远程）中当前swap设备状态 | - `host`：远程主机名/IP（本地采集可不填） | swap设备列表（含`name`swap空间对应的设备或文件路径、`type`swap空间的类型、`size`swap空间的总大小、`used`当前已使用的swap空间量、`prio`swap空间的优先级） |

## 三、待开发需求
