| 工具名称 | 工具功能 | 核心输入参数 | 关键返回内容 |
| ---- | ---- | ---- | ---- |
| `numa_bind_tool` | 在目标设备（本地/远程）上启动进程时绑定进程到指定 NUMA 节点，避免跨节点 | - `host`：远程主机名/IP（本地采集可不填）、`numa_node`指定的 NUMA 节点编号（整数）、`memory_node`指定的内存节点编号（整数）、`program_path`要运行的程序路径 | `stdout`程序的标准输出、`stderr`标准错误、`exit_code`退出状态码 |