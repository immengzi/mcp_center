# 进程启动绑定指定NUMA节点场景MCP

## 一、服务介绍

本服务是一款基于`numactl`命令实现启动时绑定进程到指定 NUMA 节点MCP，核心功能为启动时绑定进程到指定 NUMA 节点。

## 二、核心工具信息

| 类别 | 详情 |
| ---- | ---- |
| 工具名称 | numa_bind |
| 工具功能 | 通过`numactl`命令，实现启动时绑定进程到指定 NUMA 节点，把 CPU 和内存都限制在同一 NUMA 节点，避免跨节点。

## 三、待开发需求
