| 工具名称                          | 工具功能                         | 核心输入参数                             | 关键返回内容                                                                                                                          |
| ----------------------------- | ---------------------------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `perf_interrupt_health_check` | 检查系统中断统计信息，以定位高频中断导致的 CPU 占用 | - `host`：远程主机名称或 IP 地址，若不提供则获取本机信息 | 返回一个包含中断信息的列表，每个元素包含：`irq_number` 中断编号、`total_count` 总触发次数、`device` 设备名称、`cpu_distribution` 各 CPU 核心的中断分布、`interrupt_type` 中断类型 |
