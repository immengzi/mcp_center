# 文件删除MCP（管理控制程序）规范文档
## 一、服务介绍
本服务是一款基于`rm`命令实现文件删除的MCP（管理控制程序），核心功能为删除指定文件或文件夹。

## 二、核心工具信息
| 工具名称 | 工具功能 | 核心输入参数 | 关键返回内容 |
| ---- | ---- | ---- | ---- |
| `rm_collect_tool` | 对文件或文件夹进行删除 | - `host`：远程主机名/IP（本地采集可不填）<br>- `path`：要进行删除的文件或文件夹路径 | 布尔值，表示rm操作是否成功 |

## 三、待开发需求
