| 工具名称                    | 工具功能                                                           | 核心输入参数                                                                                                   | 关键返回内容                                                                     |
| ----------------------- | -------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| `numa_rebind_proc_tool` | 修改已运行进程的 NUMA 内存绑定，使用 migratepages 工具将进程的内存从一个 NUMA 节点迁移到另一个节点 | - `pid`：进程 ID<br>- `from_node`：当前内存所在的 NUMA 节点编号<br>- `to_node`：目标 NUMA 节点编号<br>- `host`：远程主机 IP 或名称（可选） | `status`：操作状态（success / error）<br>`message`：操作结果信息<br>`output`：命令的原始输出（如有） |
