| 工具名称            | 工具功能                                          | 核心输入参数                       | 关键返回内容                                                                                                                          |
| --------------- | --------------------------------------------- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `numa_diagnose` | 获取 NUMA 架构硬件监控信息，包括 CPU 实时频率、规格参数以及 NUMA 拓扑结构 | - `host`：远程主机地址（可选，不填则在本地执行） | - `real_time_frequencies`：各 CPU 核心实时频率 (MHz)<br>- `specifications`：CPU 规格信息（型号 / 频率范围 / NUMA 节点）<br>- `numa_topology`：NUMA 拓扑结构 |
