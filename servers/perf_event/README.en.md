| Tool Name | Tool Function | Core Input Parameters | Key Return Content |
| ---- | ---- | ---- | ---- |
| `perf_events_tool` | Utilizes **perf record / report** to quickly identify **CPU performance bottlenecks in the system or a specified process** (both local and remote) | - `host`: Remote hostname/IP (not required for local collection)<br>- `pid`: Target process ID (integer, defaults to collecting the entire system) | Performance analysis results (including `total_samples` total sample count, `event_count` event counts (cycles, etc.), `hot_functions` list of hot functions (default Top5, each containing `overhead` percentage, `command` process name, `shared_object` binary object, `symbol` function name, `symbol_type` symbol type (`.` user space/`k` kernel space)) |