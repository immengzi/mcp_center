# 文件夹创建MCP（管理控制程序）规范文档
## 一、服务介绍
本服务是一款基于`mkdir`命令实现文件夹创建的MCP（管理控制程序），核心功能为创建文件夹。

## 二、核心工具信息
| 工具名称 | 工具功能 | 核心输入参数 | 关键返回内容 |
| ---- | ---- | ---- | ---- |
| `mkdir_collect_tool` | 进行目录创建、支持批量创建、设置权限、递归创建多级目录 | - `host`：远程主机名/IP（本地采集可不填）<br>- `dir`：创建目录名 | 布尔值，表示mkdir操作是否成功 |

## 三、待开发需求
