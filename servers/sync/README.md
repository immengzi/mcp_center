# Sync命令MCP（管理控制程序）规范文档
## 一、服务介绍
本服务是一款基于`sync`命令实现将内存缓冲区数据写入磁盘的MCP（管理控制程序），核心功能为强制将内存缓冲区数据写入磁盘​​，​​确保文件系统数据的持久化。

## 二、核心工具信息
| 工具名称 | 工具功能 | 核心输入参数 | 关键返回内容 |
| ---- | ---- | ---- | ---- |
| `sync_refresh_data_tool` | 将内存缓冲区数据写入磁盘 | - `host`：远程主机名/IP（本地采集可不填） | 布尔值，表示缓冲数据是否刷新成功 |

## 三、待开发需求
